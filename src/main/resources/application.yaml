spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mathfusion?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: 0782
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    open-in-view: false

  logging:
    level:
      org.hibernate.sql: debug
      org.apache.catalina.core.ContainerBase: off

# DeepSeek 연동 주소
deepseek:
  api:
    url: http://ec2-54-180-102-105.ap-northeast-2.compute.amazonaws.com:8000


# DashScope API Key (필요할 경우 사용)
dashscope:
  api_key:

# AWS S3 관련 설정
cloud:
  aws:
    region:
      static: ap-northeast-2
    s3:
      bucket-name: example-aws-s3-bucket-service
    credentials:
      access-key: example-access-key
      secret-key: example-secret-key
    stack:
      auto: false

# Cloudinary 설정
cloudinary:
  api-key:
  api-secret:
  cloud-name:
  upload-preset:
